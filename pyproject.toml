[project]
name = "website"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "bobdebod", email = "rocryan2@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.7.7",
]

[project.scripts]
website = "website:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
